fastapi>=0.111.0
uvicorn[standard]>=0.29.0
numpy>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.7.0
# rasterio is optional — only needed for real Sentinel-2 tile I/O
# Install separately if needed: pip install rasterio
